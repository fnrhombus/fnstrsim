{
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Workspace root for fnstrsim",
  "scripts": {
    "build": "pnpm --filter fnstrsim build",
    "test": "pnpm --filter fnstrsim-tests test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20"
  }
}
